# Docker environment overrides
# Next.js reads .env.local from the mounted volume, so we must override
# NEXT_PUBLIC_* vars here to point to docker service names
NEXT_PUBLIC_DIRECTUS_URL=http://directus:8055
DIRECTUS_URL=http://directus:8055
